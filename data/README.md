Local project data live in this folder but are ignored by git.

Expected key file:

- `articles.csv`: analytic post-level file with metadata, LLM labels, and engagement counts

Optional local-only files can also live here, including raw split CSVs and derived datasets.
